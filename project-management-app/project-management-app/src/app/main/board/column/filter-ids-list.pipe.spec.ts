import { FilterIdsListPipe } from './filter-ids-list.pipe';

describe('FilterIdsListPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIdsListPipe();
    expect(pipe).toBeTruthy();
  });
});
